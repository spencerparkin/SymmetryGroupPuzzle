// MeshFragShader.txt

precision mediump float;

varying vec2 frag_texCoord;

uniform sampler2D texSampler;

void main()
{
    gl_FragColor = texture2D(texSampler, vec2(frag_texCoord.s, frag_texCoord.y));
}